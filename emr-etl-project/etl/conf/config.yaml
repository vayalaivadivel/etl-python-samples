# -----------------------------
# S3 CSV file location
# -----------------------------
s3:
  bucket: "vadivel-data-engineer"     # your bucket name
  key: "my-company-list/my-company-list.csv"  # path to CSV inside bucket

# -----------------------------
# RDS Configuration
# -----------------------------
rds:
  user: "admin"       # default username if env var not set
  password: "Admin12345!"   # default password if env var not set
  host: "etl-mysql-dev.cmx8sca08m6d.us-east-1.rds.amazonaws.com:3306"       # default endpoint if env var not set
  port: 3306
  database: "etl"